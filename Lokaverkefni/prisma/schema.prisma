// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Gpu {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("gpu")
}

model Cpu {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_type")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("cpu")
}

model Motherboard {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("motherboard")
}

model RamMemory {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("ram_memory")
}

model StorageDrive {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("storage_drive")
}

model PowerSupply {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int 
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("power_supply")
}

model CpuCooler {
  id        BigInt  @id @default(autoincrement())
  modelName String  @map("model_name")
  price     Decimal @db.Decimal(10, 2)
  stock     Int
  image_url String  @default("https://www.shutterstock.com/image-photo/beautiful-collection-pictures-various-dogs-600nw-2736810319.jpg")

  @@map("cpu_cooler")
}